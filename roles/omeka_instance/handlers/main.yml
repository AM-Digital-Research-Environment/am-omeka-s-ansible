---
# roles/omeka_instance/handlers/main.yml
# Handlers for the omeka_instance role.

- name: restart omeka instance
  ansible.builtin.shell:
    cmd: docker compose down && docker compose up -d
    chdir: "{{ omeka_instances_dir }}/{{ instance_name }}"
  listen: restart omeka instance
  changed_when: false
